---
const { cy } = Astro.props
---
<video
    loop={ true }
    autoplay={ false }
    muted={ true }
    playsinline={ true }
    preload="auto"
    class="h-full w-full object-cover"
    data-cy={ cy }
>
    <slot/>
</video>
